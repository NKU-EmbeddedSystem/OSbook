#!/bin/bash

dir=$(dirname "$0")/input

for i in "$dir"/*
do
    #提取文件名
    
    #获取文件的修改时间
    
    #格式化时间
    
    #提取文件的后缀名
    
    #进行拼接并将修改结果输出到output文件下的new.txt文件中，注意结果只需要修改后的文件名，不包括其目录名

done


